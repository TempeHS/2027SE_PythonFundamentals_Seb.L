def indexC(char):
    capitals = []
    for index, char in enumerate():
        if char.isupper():
            capitals.append(index)
        return capitals


bl = input("speak")
blah = indexC(bl)
print(blah)


# Actual code result = input("speak")
# indexed = indexC(result)
# split
